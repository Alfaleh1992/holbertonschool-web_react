# Task 0 — Creating an interface for a student

Implements \`Student\` interface and renders a table of first names and locations using Vanilla JS.

How to run locally (if Node is available):
1. \`npm install\`
2. \`npm run build\` or \`npm run start-dev\`
