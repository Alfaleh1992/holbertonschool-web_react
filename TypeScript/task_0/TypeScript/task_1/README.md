# Task 1 — Let’s build a Teacher interface

Implements `Teacher` interface with readonly `firstName`/`lastName`, optional `yearsOfExperience`, required `fullTimeEmployee` and `location`, and an index signature to allow extra props such as `contract: boolean`. Includes example `teacher3` logged to console.
